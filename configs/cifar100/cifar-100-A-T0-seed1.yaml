experiment_name: cifar-100-A-T0-seed1
seed: 1
deterministic: true
device: auto
data:
  root: data
  download: false
  validation_size: 5000
  workers: 0
model:
  name: resnet18_cifar
  num_classes: 100
training:
  epochs: 200
  batch_size: 128
  learning_rate: 0.1
  momentum: 0.9
  weight_decay: 0.0005
output_root: runs
dataset: CIFAR-100
